<!doctype html>
<html lang="es">
<head>
  <meta charset="utf-8" />
  <meta name="viewport" content="width=device-width,initial-scale=1" />
  <title>Para mi novia — Flores amarillas 🌼</title>
  <style>
    :root{
      --bg1: #fff9ec;
      --bg2: #fff3c8;
      --card: rgba(255,255,255,0.85);
      --accent: #ffdd57;
      --gold: #f6c94f;
      --shadow: rgba(0,0,0,0.12);
      --pink: #ff6b9a;
      --text: #222;
      --glass-border: rgba(255,255,255,0.6);
    }

    /* Reset */
    *{box-sizing:border-box;margin:0;padding:0}
    html,body{height:100%}
    body{
      font-family: "Segoe UI", Roboto, "Helvetica Neue", Arial, sans-serif;
      background: radial-gradient(circle at 10% 10%, #fffef6 0%, #fff9e4 20%, #fff3d2 40%, #fff2d0 60%), linear-gradient(180deg,var(--bg1),var(--bg2));
      color:var(--text);
      display:flex;
      align-items:center;
      justify-content:center;
      padding:30px;
    }

    /* Layout */
    .scene{
      width:100%;
      max-width:980px;
      min-height:560px;
      position:relative;
      border-radius:20px;
      padding:28px;
      box-shadow: 0 20px 50px var(--shadow);
      background: linear-gradient(180deg,var(--card), rgba(255,255,255,0.7));
      overflow:hidden;
      border:1px solid var(--glass-border);
      backdrop-filter: blur(6px) saturate(1.02);
    }

    /* Floating flower clusters */
    .flowers{
      position:absolute;
      inset:0;
      pointer-events:none;
    }
    .flower{
      position:absolute;
      transform-origin:center;
      will-change:transform,opacity;
      animation: float 6s ease-in-out infinite;
      filter: drop-shadow(0 6px 8px rgba(0,0,0,0.08));
    }
    .flower:nth-child(1){ left:6%; top:8%; width:120px; animation-duration:7s; }
    .flower:nth-child(2){ right:4%; top:6%; width:100px; animation-duration:5.5s; animation-delay:0.6s; }
    .flower:nth-child(3){ left:12%; bottom:6%; width:140px; animation-duration:8s; animation-delay:0.9s; }
    .flower:nth-child(4){ right:10%; bottom:12%; width:110px; animation-duration:6.5s; animation-delay:0.3s; }
    .flower:nth-child(5){ left:45%; top:-6%; width:180px; animation-duration:9s; animation-delay:0.2s; opacity:0.95; }

    @keyframes float {
      0%{ transform: translateY(0) rotate(0deg) scale(1); opacity:0.95; }
      50%{ transform: translateY(-18px) rotate(5deg) scale(1.03); opacity:1; }
      100%{ transform: translateY(0) rotate(0deg) scale(1); opacity:0.95; }
    }

    /* Card content */
    .card{
      position:relative;
      z-index:3;
      display:flex;
      gap:24px;
      align-items:center;
      justify-content:space-between;
      height:100%;
    }

    .left{
      flex:1 1 55%;
      padding:28px;
    }
    .right{
      flex: 0 0 38%;
      display:flex;
      align-items:center;
      justify-content:center;
      padding:18px;
    }

    h1{
      font-size:34px;
      line-height:1.05;
      margin-bottom:12px;
      color:#1b1b1b;
      text-shadow: 0 1px 0 rgba(255,255,255,0.6);
    }
    p.lead{
      font-size:18px;
      margin-bottom:18px;
      color:#2b2b2b;
    }

    .message-box{
      background: linear-gradient(180deg, rgba(255,255,255,0.9), rgba(255,250,240,0.85));
      border-radius:14px;
      padding:18px;
      box-shadow: 0 10px 30px rgba(0,0,0,0.06);
      border: 1px solid rgba(250,200,60,0.12);
      max-width:660px;
    }
    .message{
      font-size:16px;
      color:#111;
      letter-spacing:0.1px;
    }

    .heart{
      display:inline-block;
      margin-left:8px;
      transform-origin:center;
      animation: beat 1.1s infinite;
    }
    @keyframes beat{
      0%{ transform: scale(1); }
      50%{ transform: scale(1.12); }
      100%{ transform: scale(1); }
    }

    .right .card-flower{
      width:260px;
      height:260px;
      border-radius:20px;
      display:flex;
      align-items:center;
      justify-content:center;
      background: linear-gradient(145deg, rgba(255,250,235,0.9), rgba(255,245,210,0.9));
      box-shadow: 0 14px 40px rgba(0,0,0,0.08);
      border:1px solid rgba(255,230,120,0.22);
    }

    .btns{
      margin-top:16px;
      display:flex;
      gap:12px;
      flex-wrap:wrap;
    }
    .btn{
      padding:10px 16px;
      border-radius:12px;
      border: none;
      cursor:pointer;
      font-weight:600;
      box-shadow: 0 8px 20px rgba(0,0,0,0.06);
      transition: transform .14s ease, box-shadow .14s ease;
    }
    .btn:active{ transform:translateY(2px) }
    .btn-primary{
      background: linear-gradient(180deg,var(--accent),var(--gold));
      color:#1b1b1b;
      border:1px solid rgba(0,0,0,0.04);
    }
    .btn-ghost{
      background:transparent;
      color:#1b1b1b;
      border:1px solid rgba(0,0,0,0.06);
      backdrop-filter: blur(4px);
    }

    footer.note{
      position:absolute;
      left:18px;
      right:18px;
      bottom:12px;
      font-size:12px;
      color:rgba(20,20,20,0.6);
      text-align:center;
    }

    /* Responsive */
    @media (max-width:820px){
      .card{ flex-direction:column; gap:16px; align-items:flex-start; }
      .right{ width:100%; }
      .right .card-flower{ width:100%; height:220px; border-radius:14px; }
      h1{ font-size:26px }
    }
  </style>
</head>
<body>
  <div class="scene" role="main" aria-label="Tarjeta con flores amarillas para mi novia">
    <div class="flowers" aria-hidden="true">
      <!-- Repetitive floral SVGs positioned absolutely via CSS -->
      <div class="flower" style="--i:1">
        <!-- simple stylized yellow flower SVG -->
        <svg viewBox="0 0 120 120" width="100%" height="100%" xmlns="http://www.w3.org/2000/svg" aria-hidden="true">
          <defs>
            <radialGradient id="g1" cx="40%" cy="30%">
              <stop offset="0%" stop-color="#fff7b8"/>
              <stop offset="100%" stop-color="#ffd84b"/>
            </radialGradient>
          </defs>
          <g transform="translate(10,10) scale(0.8)">
            <circle cx="50" cy="50" r="14" fill="#ffcf3f"/>
            <!-- petals -->
            <g fill="#ffd94a" stroke="#f3b72b" stroke-width="1">
              <ellipse cx="50" cy="20" rx="12" ry="24" transform="rotate(0 50 50)"/>
              <ellipse cx="50" cy="80" rx="12" ry="24" transform="rotate(180 50 50)"/>
              <ellipse cx="20" cy="50" rx="12" ry="24" transform="rotate(-90 50 50)"/>
              <ellipse cx="80" cy="50" rx="12" ry="24" transform="rotate(90 50 50)"/>
              <ellipse cx="28" cy="28" rx="12" ry="24" transform="rotate(-45 50 50)"/>
              <ellipse cx="72" cy="28" rx="12" ry="24" transform="rotate(45 50 50)"/>
              <ellipse cx="28" cy="72" rx="12" ry="24" transform="rotate(45 50 50)"/>
              <ellipse cx="72" cy="72" rx="12" ry="24" transform="rotate(-45 50 50)"/>
            </g>
            <circle cx="50" cy="50" r="10" fill="url(#g1)" opacity="0.98"/>
          </g>
        </svg>
      </div>

      <div class="flower" style="--i:2">
        <svg viewBox="0 0 140 140" xmlns="http://www.w3.org/2000/svg" aria-hidden="true">
          <defs>
            <linearGradient id="g2" x1="0" x2="1" y1="0" y2="1">
              <stop offset="0%" stop-color="#fff6c6"/>
              <stop offset="100%" stop-color="#ffd34d"/>
            </linearGradient>
          </defs>
          <g transform="translate(10,10) scale(0.9)">
            <circle cx="65" cy="65" r="16" fill="#ffd04b"/>
            <g fill="#ffe37a" stroke="#f0b73b">
              <path d="M65 8 C85 8 88 30 95 28 C102 26 105 8 125 8 C105 22 112 48 95 52 C78 56 68 86 65 96 C62 86 52 56 35 52 C18 48 25 22 5 8 C25 8 28 26 35 28 C42 30 45 8 65 8Z"/>
            </g>
            <circle cx="65" cy="65" r="12" fill="#fff7b0" opacity="0.96"/>
          </g>
        </svg>
      </div>

      <div class="flower" style="--i:3">
        <svg viewBox="0 0 160 160" xmlns="http://www.w3.org/2000/svg" aria-hidden="true">
          <g transform="translate(10,10) scale(0.9)">
            <circle cx="75" cy="75" r="20" fill="#ffd34b" />
            <g fill="#ffea81" stroke="#f1c34a" stroke-width="0.8">
              <!-- small petals -->
              <ellipse cx="75" cy="22" rx="10" ry="28" />
              <ellipse cx="75" cy="128" rx="10" ry="28" />
              <ellipse cx="25" cy="75" rx="10" ry="28" transform="rotate(-90 25 75)"/>
              <ellipse cx="125" cy="75" rx="10" ry="28" transform="rotate(90 125 75)"/>
              <ellipse cx="35" cy="35" rx="10" ry="26" transform="rotate(-45 35 35)"/>
              <ellipse cx="115" cy="35" rx="10" ry="26" transform="rotate(45 115 35)"/>
              <ellipse cx="35" cy="115" rx="10" ry="26" transform="rotate(45 35 115)"/>
              <ellipse cx="115" cy="115" rx="10" ry="26" transform="rotate(-45 115 115)"/>
            </g>
            <circle cx="75" cy="75" r="12" fill="#fff9c7" opacity="0.98"/>
          </g>
        </svg>
      </div>

      <div class="flower" style="--i:4">
        <svg viewBox="0 0 120 120" xmlns="http://www.w3.org/2000/svg" aria-hidden="true">
          <g transform="translate(8,8) scale(0.85)">
            <circle cx="50" cy="50" r="10" fill="#ffd24a"/>
            <g fill="#ffe07a" stroke="#f2bb3d">
              <path d="M50 6 C60 22 84 20 94 34 C106 50 84 56 80 70 C76 84 64 92 50 94 C36 92 24 84 20 70 C16 56 -6 50 6 34 C16 20 40 22 50 6Z"/>
            </g>
            <circle cx="50" cy="50" r="8" fill="#fff6c6"/>
          </g>
        </svg>
      </div>

      <div class="flower" style="--i:5">
        <svg viewBox="0 0 200 200" xmlns="http://www.w3.org/2000/svg" aria-hidden="true">
          <g transform="translate(10,10) scale(0.9)">
            <circle cx="95" cy="95" r="22" fill="#ffd64b"/>
            <g fill="#ffe99a" stroke="#f1bd3f">
              <ellipse cx="95" cy="40" rx="14" ry="36"/>
              <ellipse cx="95" cy="150" rx="14" ry="36"/>
              <ellipse cx="46" cy="70" rx="14" ry="36" transform="rotate(-30 46 70)"/>
              <ellipse cx="144" cy="70" rx="14" ry="36" transform="rotate(30 144 70)"/>
              <ellipse cx="46" cy="120" rx="14" ry="36" transform="rotate(30 46 120)"/>
              <ellipse cx="144" cy="120" rx="14" ry="36" transform="rotate(-30 144 120)"/>
            </g>
            <circle cx="95" cy="95" r="14" fill="#fff7c6" opacity="0.96"/>
          </g>
        </svg>
      </div>
    </div>

    <div class="card" role="article">
      <div class="left">
        <h1>Pa' la más querida 🌼</h1>
        <p class="lead">Mi amor, esto es pa' ti. Flores amarillas pa' iluminar tu día y recordarte lo especial que tú eres.</p>

        <div class="message-box" id="messageBox">
          <p class="message" id="messageText">
            Mi cielo, cada vez que yo veo una flor amarilla me acuerdo de tu sonrisa. Gracias por ser mi calorcito y mi locura favorita. Te amo un montón. <span class="heart">💖</span>
          </p>

          <div class="btns" aria-hidden="false">
            <button class="btn btn-primary" onclick="surprise()">
              Enviar sorpresa ✨
            </button>
            <button class="btn btn-ghost" onclick="changeMsg()">
              Cambiar mensaje
            </button>
          </div>
        </div>

        <p style="margin-top:14px;font-size:13px;color:rgba(10,10,10,0.55);">
          Consejo: dale click a "Cambiar mensaje" si quieres escribir algo tuyo.
        </p>
      </div>

      <div class="right" aria-hidden="true">
        <div class="card-flower" title="Ramo bonito">
          <!-- A larger central composed SVG bouquet -->
          <svg viewBox="0 0 220 220" width="85%" height="85%" xmlns="http://www.w3.org/2000/svg" aria-hidden="true">
            <g transform="translate(10,10) scale(0.95)">
              <!-- stems -->
              <path d="M110 160 C108 120 90 120 92 80" stroke="#7aa05a" stroke-width="6" fill="none" stroke-linecap="round" />
              <path d="M120 160 C122 120 140 120 138 80" stroke="#6c9a4f" stroke-width="6" fill="none" stroke-linecap="round" />
              <!-- flowers cluster -->
              <g transform="translate(40,40) scale(0.9)">
                <circle cx="60" cy="40" r="14" fill="#ffd24a"/>
                <g fill="#ffe37a" stroke="#f0b73b">
                  <ellipse cx="60" cy="10" rx="10" ry="22" />
                  <ellipse cx="60" cy="70" rx="10" ry="22" />
                  <ellipse cx="32" cy="40" rx="10" ry="22" transform="rotate(-90 32 40)"/>
                  <ellipse cx="88" cy="40" rx="10" ry="22" transform="rotate(90 88 40)"/>
                </g>
                <circle cx="60" cy="40" r="8" fill="#fff8c9"/>
              </g>
              <g transform="translate(110,30) scale(0.8)">
                <circle cx="20" cy="40" r="12" fill="#ffd64b"/>
                <g fill="#ffea81" stroke="#f1c34a">
                  <ellipse cx="20" cy="10" rx="8" ry="18"/>
                  <ellipse cx="20" cy="70" rx="8" ry="18"/>
                  <ellipse cx="-8" cy="40" rx="8" ry="18" transform="rotate(-90 -8 40)"/>
                  <ellipse cx="48" cy="40" rx="8" ry="18" transform="rotate(90 48 40)"/>
                </g>
              </g>
            </g>
          </svg>
        </div>
      </div>
    </div>

    <footer class="note">Hecho con cariño por tu pana. Dale "Enviar sorpresa" pa' ver un detallito.</footer>
  </div>

  <script>
    // Small interactive bits
    function surprise(){
      const txt = document.getElementById('messageText');
      txt.innerHTML = '¡Sorpresa! 🎉 Te mando mil besos y un abrazo gigante. Te adoro, mi reina. <span class="heart">💖</span>';
      // tiny confetti effect (simple DOM dots)
      for(let i=0;i<22;i++){
        const d = document.createElement('div');
        d.style.position='fixed';
        d.style.left=(50 + (Math.random()*40-20))+'%';
        d.style.top=(40 + (Math.random()*20-10))+'%';
        d.style.width='10px';
        d.style.height='10px';
        d.style.borderRadius='50%';
        d.style.zIndex=9999;
        d.style.pointerEvents='none';
        const colors=['#ffd34d','#ffecb3','#ffb86b','#ffdd85','#fff7c8'];
        d.style.background=colors[Math.floor(Math.random()*colors.length)];
        d.style.opacity=0.95;
        d.style.transform='translateY(0) scale(0.9)';
        d.style.transition='transform 1s ease-out, opacity 1s ease-out';
        document.body.appendChild(d);
        setTimeout(()=>{ d.style.transform='translateY(140px) scale(1.4)'; d.style.opacity=0; }, 50 + i*30);
        setTimeout(()=>{ d.remove(); }, 1200 + i*20);
      }
    }

    function changeMsg(){
      const userMsg = prompt("Escribe tu mensaje pa' ella (máx 300 chars):", "Eres mi sol amarillo 🌼");
      if(userMsg === null) return;
      const sanitized = String(userMsg).slice(0,300).replace(/</g,'&lt;').replace(/>/g,'&gt;');
      document.getElementById('messageText').innerHTML = sanitized + ' <span class="heart">💖</span>';
    }

    // gentle pulse for flowers based on reduced motion preference
    if(window.matchMedia("(prefers-reduced-motion: reduce)").matches){
      document.querySelectorAll('.flower').forEach(f=> f.style.animation='none');
      document.querySelectorAll('.heart').forEach(h=> h.style.animation='none');
    }
  </script>
</body>
</html>
